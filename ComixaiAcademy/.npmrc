# three は 0.162 に固定している（r163でWebGL 1対応が消え、ネイティブのexpo-glで
# 3Dが全滅するため → CLAUDE.md）。expo-three@8 の peer は ^0.166 なので、
# そのままだと npm ci が ERESOLVE で止まる（ActionsでもEASのMacでも）。
# peerの食い違いを警告止まりにして通す。
legacy-peer-deps=true
